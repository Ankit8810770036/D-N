# Backend API URL (Leave empty or set to /api if using reverse proxy like Nginx)
# For cross-origin deployment (e.g. Vercel frontend + Laravel Forge backend):
# VITE_API_BASE_URL=https://your-api-domain.com/api

VITE_API_BASE_URL=
